name: Ping Railway to Wake Bot

on:
  schedule:
    - cron: '0 5 * * *'  # 7:00 Italian time (Rome is UTC+2 in summer)
  workflow_dispatch:      # ti permette anche di avviarlo manualmente da GitHub

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Railway
        run: curl https://ubot-production-d04e.up.railway.app/
